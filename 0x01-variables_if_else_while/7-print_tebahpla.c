#include <stdio.h>

/**
 * main - program starts
 * Return: returns 0
 */

int main(void)
{
int alphabet;
for (alphabet = 'z'; alphabet >= 'a'; alphabet--)
putchar(alphabet);
putchar('\n');
return (0);

}
