#include <stdio.h>

/**
 * main - program starts
 * Return: returns 0
 */

int main(void)
{
int alphabet;

; for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
putchar(alphabet);
for (alphabet = 'A'; alphabet  <= 'Z'; alphabet++)
putchar(alphabet);
printf("\n");
return (0);
}
