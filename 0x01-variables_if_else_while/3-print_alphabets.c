#include <stdio.h>

/**
 * main - program starts
 * Return: returns 0
 */

int main(void)
{
char alphabet;
char ALPHABET;
for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
putchar(alphabet)
for (ALPHABET = 'A'; ALPHABET <= 'Z'; ALPHABET++)
putchar(ALPHABET);
printf("\n");
return (0);
}
