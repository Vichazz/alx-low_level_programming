#include <stdio.h>

/**
 * main - program entry
 * Return: returns 0
 */

int main(void)
{
int digits;
p = (char) digits;
 
for (digits = 0; digits < 10; digits++)
putchar(p);
putchar('\n');
return (0);

}
